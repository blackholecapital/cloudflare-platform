[project]
name = "blackhole-livekit-avatar-agent"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "livekit-agents[lemonslice,openai]>=1.7.0",
  "livekit-plugins-noise-cancellation>=0.3",
  "python-dotenv>=1.0",
]
